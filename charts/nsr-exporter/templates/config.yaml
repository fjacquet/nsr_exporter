{{- if and (not .Values.existingSecret) .Values.config }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nsr-exporter.fullname" . }}-config
  labels:
    {{- include "nsr-exporter.labels" . | nindent 4 }}
stringData:
  config.yaml: {{ tpl .Values.config . | quote }}
{{- end }}
