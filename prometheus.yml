global:
  scrape_interval: 30s
  scrape_timeout: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: nsr_exporter
    static_configs:
      - targets: ['nsr_exporter:9447']
